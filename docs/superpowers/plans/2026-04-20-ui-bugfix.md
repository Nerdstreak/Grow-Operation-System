# UI Bug-Fix Paket — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 5 visuelle Bugs beheben: Dark Mode, Kamera-Proportionen, Metric-Boxen, Einstellungen-Navigation, Wissen-Stage-Karten.

**Architecture:** Nur CSS- und Razor-Änderungen. Keine neuen Services, keine DB-Änderungen, keine neuen Dateien (außer bestehende werden editiert).

**Tech Stack:** Blazor Server (.NET 8), CSS Custom Properties, vanilla JS (site.js)

---

## Betroffene Dateien

| Datei | Änderung |
|-------|----------|
| `GrowDiary.Web/Components/Layout/Sidebar.razor` | localStorage-Key fix |
| `GrowDiary.Web/wwwroot/css/site.css` | 3 CSS-Fixes + neue Settings/Wissen-Stile |
| `GrowDiary.Web/Components/Pages/Einstellungen.razor` | Sticky Nav + id-Attribute |

---

## Task 1: Dark Mode — localStorage Key fix

**Files:**
- Modify: `GrowDiary.Web/Components/Layout/Sidebar.razor:67,75`

- [ ] **Step 1: Fix Key in OnAfterRenderAsync**

In `Sidebar.razor` Zeile 67, ändere:
```csharp
// vorher
_theme = await JS.InvokeAsync<string?>("localStorage.getItem", "theme") ?? "dark";
// nachher
_theme = await JS.InvokeAsync<string?>("localStorage.getItem", "growdiary-theme") ?? "dark";
```

- [ ] **Step 2: Fix Key in ToggleTheme**

In `Sidebar.razor` Zeile 75, ändere:
```csharp
// vorher
await JS.InvokeVoidAsync("localStorage.setItem", "theme", _theme);
// nachher
await JS.InvokeVoidAsync("localStorage.setItem", "growdiary-theme", _theme);
```

- [ ] **Step 3: Build-Check**

```bash
dotnet build GrowDiary.Web/GrowDiary.Web.csproj -m:1 -v:minimal
```
Erwartet: Build succeeded, 0 errors.

- [ ] **Step 4: Manuell testen**

App starten, Theme-Button klicken → Seite wird hell. Seite neu laden → bleibt hell. Nochmal klicken → dunkel, bleibt dunkel nach Reload.

- [ ] **Step 5: Commit**

```bash
git add GrowDiary.Web/Components/Layout/Sidebar.razor
git commit -m "fix: dark mode — localStorage key 'theme' → 'growdiary-theme'"
```

---

## Task 2: Kamera-Proportionen fix

**Files:**
- Modify: `GrowDiary.Web/wwwroot/css/site.css:222`

- [ ] **Step 1: CSS-Zeile entfernen**

In `site.css` Zeile 222, diese Zeile komplett entfernen:
```css
.tent-card .cam-strip > div { height: 180px !important; aspect-ratio: unset !important; }
```

Der `.cam-strip` und `.cam-placeholder` behalten ihre bestehenden Stile (`aspect-ratio: 16/9`, `overflow: hidden`), die korrekt sind. Kein Ersatz nötig.

- [ ] **Step 2: Sicherstellen dass cam-strip volle Breite hat**

In `site.css`, suche den Block `.cam-strip`. Er steht bei ca. Zeile 232. Stelle sicher `width: 100%` ist gesetzt — falls nicht, ergänzen:
```css
.cam-strip { position: relative; border-top: 1px solid var(--border); overflow: hidden; width: 100%; }
```

- [ ] **Step 3: Build-Check**

```bash
dotnet build GrowDiary.Web/GrowDiary.Web.csproj -m:1 -v:minimal
```
Erwartet: Build succeeded, 0 errors.

- [ ] **Step 4: Commit**

```bash
git add GrowDiary.Web/wwwroot/css/site.css
git commit -m "fix: camera aspect-ratio — remove !important override in tent-card"
```

---

## Task 3: Metric-Boxen — minmax fix + Wissen Stage-Karten

**Files:**
- Modify: `GrowDiary.Web/wwwroot/css/site.css:209,423-428`

- [ ] **Step 1: Metric-Grid minmax erhöhen**

In `site.css` Zeile 209, ändere:
```css
/* vorher */
.tc-metrics-row { display: grid; grid-template-columns: repeat(auto-fill, minmax(130px, 1fr)); border-bottom: 1px solid var(--border); }
/* nachher */
.tc-metrics-row { display: grid; grid-template-columns: repeat(auto-fill, minmax(160px, 1fr)); border-bottom: 1px solid var(--border); }
```

- [ ] **Step 2: Wissen Stage-Karten vergrößern**

In `site.css` Zeile 423, ändere:
```css
/* vorher */
.wissen-stages { display: grid; grid-template-columns: repeat(auto-fill, minmax(120px, 1fr)); gap: 8px; margin-bottom: 4px; }
/* nachher */
.wissen-stages { display: grid; grid-template-columns: repeat(auto-fill, minmax(180px, 1fr)); gap: 10px; margin-bottom: 4px; }
```

In `site.css` Zeile 424, ändere:
```css
/* vorher */
.wissen-stage-card { padding: 14px 16px; background: var(--surface); border: 1px solid var(--border); border-radius: 8px; }
/* nachher */
.wissen-stage-card { padding: 16px 20px; background: var(--surface); border: 1px solid var(--border); border-radius: 8px; }
```

In `site.css` Zeile 426, ändere:
```css
/* vorher */
.wissen-stage-dose   { font-family: var(--mono); font-size: 14px; font-weight: 300; color: var(--text); line-height: 1.2; }
/* nachher */
.wissen-stage-dose   { font-family: var(--mono); font-size: 15px; font-weight: 400; color: var(--text); line-height: 1.3; }
```

- [ ] **Step 3: Build-Check**

```bash
dotnet build GrowDiary.Web/GrowDiary.Web.csproj -m:1 -v:minimal
```
Erwartet: Build succeeded, 0 errors.

- [ ] **Step 4: Commit**

```bash
git add GrowDiary.Web/wwwroot/css/site.css
git commit -m "fix: metric grid minmax 130→160px, wissen stage cards larger"
```

---

## Task 4: Einstellungen — Sticky Seitenmenü

**Files:**
- Modify: `GrowDiary.Web/Components/Pages/Einstellungen.razor`
- Modify: `GrowDiary.Web/wwwroot/css/site.css`

- [ ] **Step 1: CSS — settings-layout + settings-nav hinzufügen**

In `site.css`, suche die Zeile `.settings-grid` (ca. Zeile 388). Ersetze:
```css
/* vorher */
.settings-grid  { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; align-items: start; }
```
durch:
```css
/* nachher */
.settings-layout { display: grid; grid-template-columns: 160px 1fr; gap: 0 24px; align-items: start; }
.settings-nav { position: sticky; top: 0; padding-top: 4px; }
.settings-nav-list { list-style: none; padding: 0; margin: 0; display: flex; flex-direction: column; gap: 2px; }
.settings-nav-link { display: block; padding: 8px 12px; font-size: 12px; font-weight: 500; color: var(--muted); text-decoration: none; border-radius: 6px; border-left: 2px solid transparent; transition: color 0.1s, background 0.1s, border-color 0.1s; }
.settings-nav-link:hover { color: var(--text); background: var(--surface2); }
.settings-nav-link.is-active { color: var(--text); border-left-color: var(--green); background: var(--surface2); }
.settings-grid  { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; align-items: start; }
```

- [ ] **Step 2: Einstellungen.razor — Layout-Wrapper + Nav + id-Attribute**

In `Einstellungen.razor`, ändere den äußeren Container. Suche:
```razor
<div class="page-scroll">
    <div class="settings-grid">
```
Ersetze durch:
```razor
<div class="page-scroll">
    <div class="settings-layout">
        <nav class="settings-nav">
            <ul class="settings-nav-list">
                <li><a class="settings-nav-link" href="#ha">Home Assistant</a></li>
                <li><a class="settings-nav-link" href="#zelte">Zelte</a></li>
                <li><a class="settings-nav-link" href="#systeme">Hydro-Systeme</a></li>
                <li><a class="settings-nav-link" href="#datenbank">Datenbank</a></li>
                <li><a class="settings-nav-link" href="#app">App</a></li>
            </ul>
        </nav>
        <div class="settings-grid">
```

- [ ] **Step 3: id-Attribute an die Sections**

In `Einstellungen.razor`, füge `id`-Attribute zu den 5 Sections hinzu:

`@* Home Assistant *@` → `<div id="ha">` (vorher `<div>`)

`@* Zelte *@` → `<div id="zelte">` (vorher `<div>`)

`@* Hydro-Systeme *@` → `<div id="systeme">` (vorher `<div>`)

`@* Datenbank *@` → `<div id="datenbank">` (vorher `<div>`)

`@* App *@` → `<div id="app">` (vorher `<div>`)

- [ ] **Step 4: Schließende Div ergänzen**

Am Ende von `Einstellungen.razor`, vor dem letzten `</div>` (`</div>` für `page-scroll`), stelle sicher dass der neue `settings-layout`-Wrapper korrekt geschlossen wird. Das Ende soll so aussehen:
```razor
        </div>  @* settings-grid *@
    </div>      @* settings-layout *@
</div>          @* page-scroll *@
```

- [ ] **Step 5: Build-Check**

```bash
dotnet build GrowDiary.Web/GrowDiary.Web.csproj -m:1 -v:minimal
```
Erwartet: Build succeeded, 0 errors.

- [ ] **Step 6: Manuell testen**

App öffnen, `/einstellungen` aufrufen. Links erscheint das Menü mit 5 Punkten. Klick auf "Hydro-Systeme" scrollt zur Sektion. Klick auf "Datenbank" scrollt zur Datenbank-Sektion.

- [ ] **Step 7: Commit**

```bash
git add GrowDiary.Web/Components/Pages/Einstellungen.razor GrowDiary.Web/wwwroot/css/site.css
git commit -m "feat: Einstellungen — sticky Seitenmenü mit Anker-Navigation"
```

---

## Self-Review

**Spec-Abdeckung:**
- Bug 1 (Dark Mode key) → Task 1 ✓
- Bug 2 (Kamera) → Task 2 ✓
- Bug 3 (Metric-Boxen) → Task 3 ✓
- Bug 4 (Einstellungen Nav) → Task 4 ✓
- Bug 5 (Wissen Stage-Karten) → Task 3 ✓

**Placeholder-Scan:** Keine TBDs, keine offenen Implementierungen.

**Typ-Konsistenz:** Nur CSS und Razor-Strings — keine Typ-Konflikte möglich.
