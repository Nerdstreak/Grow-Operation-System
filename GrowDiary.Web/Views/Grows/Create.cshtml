@model GrowFormViewModel
@{
    ViewData["Title"] = "Neuer Grow";
}
<section class="page-hero glass-panel wizard-hero single-column">
    <div>
        <span class="eyebrow">Setup Wizard</span>
        <h1>Neuen Grow aufsetzen</h1>
    </div>
</section>

<article class="glass-panel form-card top-gap-lg">
    @await Html.PartialAsync("_GrowForm", Model)
</article>
