# Shell scripts must keep LF endings so they run on Linux / Raspberry Pi
# even when committed from Windows.
*.sh text eol=lf

# Windows-native scripts keep CRLF
*.ps1 text eol=crlf
*.cmd text eol=crlf
*.bat text eol=crlf
